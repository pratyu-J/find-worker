<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.win7.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".hirerReg" />
        <activity android:name=".hireeReg" />
        <activity android:name=".hirerLog" />
        <activity android:name=".hireeLog" />
        <activity android:name=".hireeProfile" />
        <activity android:name=".hireeRegPage2" />
        <activity android:name=".hireeProfileView" />
        <activity android:name=".hirerQuerryPage" />
    </application>

</manifest>